# Fixture record a13__202601040404

This standard A13 record proves that the renumbered startup template uses the
normal two-digit run-code format.

The checker must exclude this dated record from mechanical document checks:
[intentionally broken historical link](./missing-standard-a13-target.md).
